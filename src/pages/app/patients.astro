---
import AppLayout from '@/layouts/AppLayout.astro';

const user = Astro.locals.user;
if (!user) {
  return Astro.redirect('/login');
}
---

<AppLayout title="Pacientes" description="Historial clínico y registros" user={user}>
  <div class="rounded-3xl border border-dashed border-white/10 bg-slate-950/60 p-10 text-center text-slate-300">
    <p class="text-lg font-semibold text-white">Módulo de pacientes en progreso</p>
    <p class="mt-3 text-sm">Incluiremos fichas clínicas, documentos y consentimientos electrónicos con firmas digitales.</p>
  </div>
</AppLayout>
