---
import PublicLayout from '@/layouts/PublicLayout.astro';
---

<PublicLayout title="Página no encontrada | DigitalDent" showNavLogin={false}>
  <section class="flex min-h-[60vh] items-center justify-center bg-slate-950 px-6 py-24">
    <div class="max-w-lg text-center">
      <p class="text-sm font-semibold uppercase tracking-[0.3em] text-accent">404</p>
      <h1 class="mt-4 text-3xl font-bold text-white md:text-4xl">No encontramos la página que buscas</h1>
      <p class="mt-4 text-slate-300">Verifica la URL o vuelve al inicio para seguir explorando la plataforma.</p>
      <div class="mt-8 flex flex-col gap-3 sm:flex-row sm:justify-center">
        <a href="/" class="rounded-full bg-primary px-6 py-3 text-sm font-semibold text-white hover:bg-primary/90">Ir a la home</a>
        <a href="/login" class="rounded-full border border-white/10 px-6 py-3 text-sm font-semibold text-slate-200 hover:border-accent hover:text-accent">Acceder al panel</a>
      </div>
    </div>
  </section>
</PublicLayout>
